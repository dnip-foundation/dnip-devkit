{
  "gateway": {
    "middlewares": ["mw.some"],
    "routes": [
      {
        "middlewares": ["mw.headers"],
        "path": "/",
        "aliases": {
          "GET /some": ["domain.some", "mw.jwt", "mw.some"]
        }
      }
    ]
  }
}
