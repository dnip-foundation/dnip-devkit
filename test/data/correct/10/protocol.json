{
  "gateway": {
    "middlewares": ["mw.some"],
    "routes": [
      {
        "middlewares": ["mw.headers"],
        "path": "/",
        "aliases": {
          "GET /some": ["mw.jwtBefore", "mw.someBefore", "domain.some", "mw.jwtAfter", "mw.someAfter"]
        }
      }
    ]
  }
}
