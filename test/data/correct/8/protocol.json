{
  "gateway": {
    "middlewares": ["mw.some"],
    "routes": [
      {
        "middlewares": ["mw.headers"],
        "path": "/",
        "aliases": {
          "GET /some": ["mw.jwt", "mw.some", "domain.some"]
        }
      }
    ]
  }
}
